# Frontend (Vite) — loyiha ildizi
VITE_API_URL=http://127.0.0.1:8787

# Docker (ixtiyoriy; backend/.env dagi 5173 o'rniga ishlatiladi)
# WEB_PORT=8080
# FRONTEND_ORIGIN=http://SIZNING_IP:8080

# Go API, Stripe, JWT — backend/.env.example dan: cp backend/.env.example backend/.env
