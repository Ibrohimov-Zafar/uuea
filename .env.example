# Frontend (Vite) — lokal dev
VITE_API_URL=http://127.0.0.1:8787

# Docker: VITE_API_URL odatda /api (nginx proxy). VPS IP shart emas.
# FRONTEND_ORIGIN=http://SIZNING_IP:8080  — Stripe redirect uchun

# Go API, Stripe, JWT — backend/.env.example dan: cp backend/.env.example backend/.env
