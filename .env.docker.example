# Docker Compose uchun (nusxa: cp .env.docker.example .env.docker)

API_PORT=8787
WEB_PORT=8080

# Brauzer API ga shu manzil orqali murojaat qiladi (host port)
VITE_API_URL=http://localhost:8787

FRONTEND_ORIGIN=http://localhost:8080
JWT_SECRET=dev_change_me_change_in_production

# Stripe (ixtiyoriy — to'lovsiz rejimda bo'sh qoldirish mumkin)
STRIPE_SECRET_KEY=
